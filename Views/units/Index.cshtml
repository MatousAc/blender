@model IEnumerable<blender.Models.unit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.unit_system.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.unit_type.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.unit_system.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.unit_type.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.name }) |
            @Html.ActionLink("Details", "Details", new { id=item.name }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.name })
        </td>
    </tr>
}

</table>
